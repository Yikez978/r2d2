<% provide(:title, "Layer 2 Sweeper Service") %>
<% provide(:description, @node.mac) %>
<table class="table table-striped">
  <thead><tr><th>IP</th><th>Sweep</th></tr></thead>
  <%= render @sweeps %>
</table>
<%= will_paginate @sweeps%>