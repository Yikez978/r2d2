<% if @sweeper.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-danger">
      The form contains <%= pluralize(@sweeper.errors.count, "error") %>.
    </div>
    <ul>
    <% @sweeper.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>