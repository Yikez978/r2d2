<div class='col-md-4 col-md-offset-2'>
  <%= form_for(@list, :html => { :class => "form-horizontal" }) do |f| %>
    <%= render 'error_messages' %>
  <div class="form-group">
    <%= f.label :name, class: 'col-sm-3' %>
    <div class='col-sm-9'>
      <%= f.text_field :name, class: 'form-control' %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :glyph, class: 'col-sm-3' %>
    <div class='col-sm-9'>
      <%= f.select :glyph_id, Glyph.all.collect { |g| [g.name, g.id] } %>
    </div>
  </div>
    <%= f.submit "Save", class: "btn btn-primary pull-right" %>
    <%= link_to 'Cancel', lists_path, class: 'btn btn-default pull-right'%>
  <% end %>
</div>
