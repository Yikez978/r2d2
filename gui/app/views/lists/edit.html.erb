<% provide(:title, "Remote Rogue Device Detector") %>
<% provide(:description, 'Edit List') %>
<%= form_for(@list) do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name, class: 'form-control' %>
  <%= f.submit "Save changes", class: "btn btn-primary" %>
<% end %>
